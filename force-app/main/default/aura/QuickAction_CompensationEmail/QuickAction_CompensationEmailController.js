({
    init : function(component, event, helper)
    {
        helper.sendCompensationEmail(component, event);
    },

    // cancel : function(component, event, helper)
    // {
    //     helper.closeQuickAction();    
    // }
})