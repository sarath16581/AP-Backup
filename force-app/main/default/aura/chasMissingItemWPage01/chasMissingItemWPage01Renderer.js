({
    /*afterRender: function (component, helper) {
        this.superAfterRender();
        //-- Console log
        if(component.get("v.debugMode")){
            console.log('MissingWizard:cmp page02 after renderer.........');
        }
        component.set('v.showOrHideAllFieldVlidationError', false);
         if(component.get("v.debugMode")){
            console.log('no need to validate all fields');
        }
       // var isValid = helper.validateFormAndShowErrors(component);
        
    }, */
})