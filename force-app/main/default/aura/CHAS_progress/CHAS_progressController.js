({
	/*doInit : function(cmp, event, helper) {
		
		var firstWord = cmp.get("v.progressFirstWord");
		var stepOne = (firstWord + " details");
		var stepTwo = "Contact details";
		var stepThree = "Review and submit";

		var currentstepName = cmp.get("v.currentStepName");
		if (cmp.get("v.currentStepNumber") == 1) {
			var currentStepName = stepOne;
		}
		else if (cmp.get("v.currentStepNumber") == 2) {
			var currentStepName = stepTwo;
		}
		else {
			var currentStepName = stepThree;
		}

		cmp.set("v.currentStepName", currentStepName);

	}*/
})