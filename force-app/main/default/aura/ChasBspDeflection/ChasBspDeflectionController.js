({
    closeBox: function(cmp, event, helper) {
        // var cmpTarget = cmp.find('bspBox');
        // $A.util.removeClass(cmp, 'animate-open-true');
        $A.util.addClass(cmp, 'slds-hide');
    },    
})