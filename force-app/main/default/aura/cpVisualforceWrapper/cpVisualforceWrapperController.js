/**
 *  2018-05-15 Hasantha.Liyanage@auspost.com.au created
 */

({
    doInit : function(component, event, helper) {
        //retrive configs on load or any other init functionality
        helper.init(component, event, helper);
    }

})