@isTest
private class TestAccountAddressInformationAccExt {

    static testMethod void TestAccountAddressInformationAccExt() {
        /*Account a = TestDataProvider.createPersonAccount('MyName','PersonAccount');
        insert a;
        
        ApexPages.StandardController sc = new ApexPages.standardController(a);
        AccountAddressInformationAccExt accExt = new AccountAddressInformationAccExt(sc);
        List<Schema.FieldSetMember> fields = accExt.getFields();
        List<Schema.FieldSetMember> otherFields = accExt.getOtherFields();
        accExt.saveAccount();
        accExt.cancel();*/
    }
}