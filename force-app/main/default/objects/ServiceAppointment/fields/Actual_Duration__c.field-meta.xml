<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Actual_Duration__c</fullName>
    <description>Calculate Actual Duration.  Note that the standard field does not recalculate with Actual Start and End are updated, which is required to set these in the Transport flow for accurate times when the device is offline</description>
    <externalId>false</externalId>
    <formula>IF(AND(NOT(ISBLANK(ActualEndTime)),NOT(ISBLANK(ActualStartTime))),
  (ActualEndTime - ActualStartTime) * 1440,
  NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Actual Duration</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
