<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IsEmailEnabled__c</fullName>
    <description>This field is a back-end indicator as to whether or not the solution should allow sending of First Notice / Final Notice / other notifications via Email and SMS</description>
    <formula>IF( $CustomMetadata.ICPSSettings__mdt.ICPS_Settings.IsEmailEnabled__c, TRUE, FALSE)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This field is a back-end indicator as to whether or not the solution should allow sending of First Notice / Final Notice / other notifications via Email and SMS</inlineHelpText>
    <label>IsEmailEnabled?</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
